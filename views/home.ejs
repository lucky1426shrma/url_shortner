<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
</head>
<body>
    <h1>URL Shortner</h1>

    <% if (locals.check) { %>
        <p>the previous user has succesfully signed up</p>
    <% } %>
    
    <% if (locals.id) { %>
        <p>URL GENERATED : http://localhost/url/<%= id %> </p>
    <% } %>

    <form method="POST" action="/url"> 

        <label>Enter your original URL</label>
        <input
        type= "text"
        name="url" 
        placeholder="https://example.com"
        />
        <button type="submit"> generate </button>

    </form>

    <div>
    <br>
      <% if (locals.allurl) { %>
            <table>
                <thead>
                    <th>S.no.</th>
                    <th>Short Id</th>
                    <th>Redirect</th>
                    <th>Clicks</th>
                </thead>
                <tbody>
                    <!-- dynamic data-->
                    <% allurl.forEach((url,index)=> { %>
                        <tr>
                        <td><%= index + 1 %></td>
                        <td><%= url.shortId %> </td>
                        <td><%= url.redirectedURL %></td>
                        <td><%= url.visitHistory.length %> </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
      <% } %>  
    </div>
    
</body>
</html>